{
    // This is a template configuration file.
    // Create your own copy, e.g., `appsettings.dev.json` or `appsettings.prod.json`.

    // By default, the `Config` class uses `appsettings.dev.json`.
    // To use another file, pass the environment name, e.g., `new Config("prod")`.

    // NOTE:
    // - `appsettings.template.json` is excluded from the build.
    // - All other appsettings files are copied to the output directory.
    // - Non-template appsettings files are excluded from version control 
    //   to protect sensitive information.

    "ConnectionStrings": {
        "localhost": "Server=localhost;Database=mydb;User=myuser;Password=mypassword;"
        // Replace with your actual server, database, user, and password.
    }d
}
